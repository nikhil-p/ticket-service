package com.walmart.ticketservice.enums;

/**
 * An Enum which represents the state of any seat in the event space
 * enum state names are intuitive.
 */
public enum SeatState {
    AVAILABLE,
    ON_HOLD,
    RESERVED
}
